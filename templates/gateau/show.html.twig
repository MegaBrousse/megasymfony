{% extends 'base.html.twig' %}

{% block title %}Gateau{% endblock %}

{% block body %}
    <h1>Gateau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gateau.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ gateau.nom }}</td>
            </tr>
            <tr>
                <th>DureePrepa</th>
                <td>{{ gateau.dureePrepa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gateau_index') }}">back to list</a>

    <a href="{{ path('gateau_edit', {'id': gateau.id}) }}">edit</a>

    {{ include('gateau/_delete_form.html.twig') }}
{% endblock %}
